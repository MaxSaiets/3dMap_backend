## Elevation / DEM settings
#
# For realistic terrain you have two good options:
#
# 1) Local DEM GeoTIFF (best accuracy, no API limits)
#    Download Copernicus DEM / SRTM / LiDAR GeoTIFF for your area and point DEM_PATH to it.
#    DEM can be in any CRS; it will be sampled and reprojected automatically.
#
# DEM_PATH=H:\\path\\to\\dem.tif
#
# 2) Dynamic download (no key) via Terrarium elevation tiles (recommended default)
# ELEVATION_PROVIDER=terrarium
# TERRARIUM_URL=https://s3.amazonaws.com/elevation-tiles-prod/terrarium
# TERRARIUM_ZOOM=14
# TERRARIUM_CACHE_DIR=cache\\terrarium
# TERRARIUM_TIMEOUT=30
#
# 3) Public API (no key) via OpenTopoData (can be rate-limited)
# ELEVATION_PROVIDER=opentopodata
# OPENTOPODATA_DATASET=eudem25m   # good for Europe (often rate-limited)
# OPENTOPODATA_DATASET=srtm30m    # global fallback
# OPENTOPODATA_URL=https://api.opentopodata.org/v1
# OPENTOPODATA_BATCH=100
# OPENTOPODATA_TIMEOUT=30
#
# Debug logs
# ELEVATION_DEBUG=1

## OSM data source
# Default uses Overpass via OSMnx (good for quick tests).
# For production / “all Ukraine” best repeatability use local PBF:
#
# OSM_SOURCE=pbf
# OSM_PBF_PATH=cache\\osm\\ukraine-latest.osm.pbf
# OSM_PBF_AUTO_DOWNLOAD=1   # auto-download from Geofabrik if missing

## Building footprints (for better outlines in cities with poor OSM)
# 1) Download footprints for Ukraine (Microsoft/Google) in any supported format (GeoJSON/CSV WKT).
# 2) Convert once to a bbox-queryable GeoPackage:
#    python scripts/prepare_footprints_gpkg.py --input <downloaded_file> --output cache\\footprints\\ukraine_buildings.gpkg
# 3) Enable:
# USE_FOOTPRINTS=1
# FOOTPRINTS_GPKG_PATH=cache\\footprints\\ukraine_buildings.gpkg
# FOOTPRINTS_LAYER=buildings


